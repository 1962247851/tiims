spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: --------------
    username: -----------
    password: -----------
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    root: info
    com.example.tiims: debug
  file:
    name: tiims_dev.log
